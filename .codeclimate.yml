engines:
  duplication:
    enabled: true
    config:
      languages:
        - javascript:
  eslint:
    enabled: true
    checks:
      global-require:
        enabled: false
  fixme:
    enabled: true

ratings:
  paths:
    - "bin/*"
    - "src/*.js"

exclude_paths:
  - "LICENSE"
  - "README*"
  - "package.json"
  - "dist/"
  - "html/"
  - "test/"
