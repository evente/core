engines:
  duplication:
    enabled: true
    config:
      languages:
        - javascript:
  eslint:
    enabled: true
    checks:
      global-require:
        enabled: false
  fixme:
    enabled: true

ratings:
  paths:
    - "src/*.js"

exclude_paths:
  - "build/"
  - "html/"
  - "node/"
  - "node_modules/"
  - "tests/"
